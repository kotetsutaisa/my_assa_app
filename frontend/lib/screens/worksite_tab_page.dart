import 'package:flutter/material.dart';

class WorksiteTabPage extends StatelessWidget {
  const WorksiteTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('日報ページ'),
    );
  }
}