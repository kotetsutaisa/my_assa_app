import 'package:flutter/material.dart';

class TimelineTabPage extends StatelessWidget {
  const TimelineTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('タイムラインページ'),
    );
  }
}