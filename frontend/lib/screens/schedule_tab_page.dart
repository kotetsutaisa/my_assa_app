import 'package:flutter/material.dart';

class ScheduleTabPage extends StatelessWidget {
  const ScheduleTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('スケジュールページ'),
    );
  }
}